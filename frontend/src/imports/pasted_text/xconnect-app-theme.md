/professional /expert

IMPORTANT — STRICT SCOPE CONTROL

Make ONLY the changes explicitly requested below.

The existing XConnect application UI is already established and must be preserved.

MOST IMPORTANT:
DO NOT TOUCH THE LAUNCH / SPLASH PAGE IN ANY WAY.

The launch page is already FINAL, APPROVED, and PERFECT.

It already has:
- The correct X logo
- The correct #0F2F2C background
- The correct #33D1AC accent
- The correct animation
- The correct timing
- The correct X → Connect → XConnect sequence
- The correct final positioning
- The correct circular/radio wave animation

ABSOLUTELY NOTHING on the launch/splash page should be modified.

Do NOT recolor it.
Do NOT replace its logo.
Do NOT modify its animation.
Do NOT modify its timing.
Do NOT modify its layout.
Do NOT modify its transitions.
Do NOT modify its background.
Do NOT modify its typography.
Do NOT modify its X.
Do NOT modify its CONNECT text.
Do NOT modify its wave/radar effect.

The launch/splash page must remain EXACTLY as it currently is.

The changes below apply ONLY to the application UI AFTER the launch/splash page.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. APPLY THE XCONNECT THEME TO THE APP UI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

For the application screens AFTER the launch/splash page, use the following XConnect brand colors:

#0F2F2C — Very Dark Teal
#33D1AC — Bright Turquoise / Mint Teal
#FFFFFF — Pure White

Use these colors to theme the EXISTING application UI.

IMPORTANT:

Do NOT apply these theme changes to the launch/splash page because it is already finalized.

For the remaining application screens, adapt the existing colors to the XConnect theme while preserving the current UI design.

Use:

#0F2F2C
for appropriate primary/dark backgrounds and dark UI elements.

#33D1AC
for appropriate primary actions, accents, active states, highlights, and brand elements.

#FFFFFF
for primary text, icons, and elements requiring strong contrast.

Do NOT introduce unrelated colors unless required for basic readability or existing functional states.

DO NOT redesign the UI.

Preserve the existing:
- Screen layouts
- Component hierarchy
- Spacing
- Dimensions
- Typography
- Cards
- Buttons
- Navigation
- Headers
- Icons
- Animations
- Transitions

Only adapt the existing visual styling to the XConnect color theme.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. LOGIN PAGE — REPLACE THE TOP LOGO
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

On the existing Login / Sign In page, replace ONLY the logo currently displayed at the top with the X icon attached with this prompt.

Use the attached X icon EXACTLY as provided.

DO NOT:
- Redesign the X
- Recreate the X
- Simplify the X
- Generate another X
- Change its geometry
- Change its proportions
- Change its internal elements
- Replace it with a generic X icon

The attached X is the exact visual source of truth.

Preserve the existing logo position, size, alignment, spacing, and surrounding UI.

ONLY replace the logo asset.

IMPORTANT:
This change applies to the LOGIN PAGE ONLY.
DO NOT change the X/logo on the launch/splash page.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. LOGIN / SIGN-IN FORM
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

On the existing Login / Sign In page, replace the current "Work Email" authentication field with standard email + password login.

The existing Login page UI and layout must remain unchanged except for the requested authentication fields.

The form should contain:

Email
Placeholder:
"Enter your email"

Password
Placeholder:
"Enter your password"

Also include:

"Forgot password?"

and:

"New here? Create an account"

"Forgot password?" should function as the appropriate password-reset action/link.

"Create an account" should navigate to the account creation screen.

Preserve the existing:
- Login page layout
- Input styling
- Button styling
- Typography
- Spacing
- Alignment
- Background
- Containers
- Existing animations
- Navigation structure

Only change the authentication fields and required links.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. CREATE ACCOUNT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

When the user selects:

"New here? Create an account"

provide the Create Account / Sign Up screen.

The form should contain:

Full Name
Placeholder:
"Enter your full name"

Email
Placeholder:
"Enter your email"

Password
Placeholder:
"Create a password"

Confirm Password
Placeholder:
"Confirm your password"

Include the appropriate primary action button to create the account.

The Create Account screen must use the same existing XConnect UI language and theme.

Do NOT create an unrelated or completely new visual design.

Reuse the existing:
- Input styles
- Button styles
- Typography
- Spacing
- Containers
- Navigation
- Theme

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. REPLACE THE TOP APPLICATION NAME AFTER LOGIN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After login, the existing application screens currently display a name/branding at the top that includes the existing "cloud connected" branding.

Replace ONLY that application name/branding with:

XConnect

Apply this replacement across all authenticated/application screens where that existing application name currently appears.

The text should simply be:

XConnect

IMPORTANT:

Do NOT change the existing header structure.

Do NOT move the header.

Do NOT change its height.

Do NOT change navigation icons.

Do NOT change profile/user information.

Do NOT change spacing.

Do NOT redesign the header.

Simply replace the existing application name/branding text with:

XConnect

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. ABSOLUTE PRESERVATION RULE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Everything not explicitly mentioned in this prompt must remain untouched.

DO NOT modify:

- Launch / splash page
- Launch animation
- Launch X logo
- Launch background
- Launch CONNECT animation
- Launch XConnect animation
- Launch radar/wave animation
- Home screen structure
- Dashboard structure
- Admin UI
- Navigation
- Existing components
- Existing animations
- Existing transitions
- Existing spacing
- Existing dimensions
- Existing typography
- Existing backend
- Existing API
- Existing authentication architecture except for the requested login/signup UI
- Existing business logic
- Existing data handling
- Existing XConnect presence/connectivity functionality

DO NOT refactor unrelated code.

DO NOT optimize unrelated components.

DO NOT redesign existing screens.

DO NOT introduce new design patterns.

DO NOT make additional "improvements".

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL EXPECTED RESULT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The launch/splash page must remain 100% IDENTICAL to the current version.

Only the application UI AFTER the launch page should receive the requested changes:

1. Apply the XConnect theme:
   #0F2F2C
   #33D1AC
   #FFFFFF

2. Replace the Login page's top logo with the attached X icon.

3. Change Login from Work Email to:
   Email
   Password
   Forgot password?
   New here? Create an account

4. Add Create Account with:
   Full Name
   Email
   Password
   Confirm Password

5. Replace the existing top application branding with:
   XConnect

Everything else must remain exactly as it currently is.

FINAL WARNING:

THE LAUNCH / SPLASH PAGE IS OUT OF SCOPE.

DO NOT TOUCH IT UNDER ANY CIRCUMSTANCES.

It is already finalized and must remain exactly as it currently is.
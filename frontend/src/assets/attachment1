/professional /expert /production-grade /enterprise-level

XCONNECT — FINAL MASTER PROMPT: PRESENTER HOME CONSOLIDATION + LOGO FIX + CLEANUP

IMPORTANT:
This is an EXISTING, already-approved XConnect application. Launch/Splash, Attendee, and Admin Monitor are FINAL and PERFECT. This prompt authorizes a small, surgical set of changes ONLY — not a redesign.

DO NOT rebuild the application.
DO NOT redesign anything not explicitly listed below.
DO NOT touch, restyle, or "improve" any screen not named in this prompt.

============================================================
0. ABSOLUTE PRESERVATION RULE — READ FIRST

The following are LOCKED, FINALIZED, and APPROVED. They must remain 100% unchanged, pixel-for-pixel, in every respect (layout, color, spacing, animation, timing, copy, components, behavior):

Launch / Splash screen — the entire animation sequence, the transparent X, background color, wave/radar animation, timing, easing, and transition into the app. This is untouchable. Do not apply any theme, logo, or asset change from this prompt to the Launch screen under any circumstance.
Attendee experience — Home (Find My Room / Begin Detection + Recent Sessions), My Activity, Profile, navigation. Untouched.
Admin experience — Monitor screen and all its functionality, Profile, navigation. Untouched.
Global theme — light mode only, brand colors 
#0F2F2C / 
#33D1AC / 
#FFFFFF, existing bottom navigation visual treatment (colors/style unchanged in this pass).
Presenter's existing Roster, Analysis, Profile screens, and the existing active-hosting screen's scrollable roster-preview + "View Full Roster" behavior. Untouched.

Only the three items in sections 1–3 below are authorized changes.

============================================================

LOGO — TWO SEPARATE, NEVER-INTERCHANGED ASSETS
============================================================

Asset A — Launch X (existing). Transparent, no background. Used ONLY inside the Launch/Splash animation. Never touch it, never reuse it anywhere else.

Asset B — Application logo (attached to this prompt). A square asset: 
#0F2F2C background, an X formed from one white arm and one mint (
#33D1AC) arm, with a small dark circle and white dot at the center where the arms cross.

Use Asset B EXACTLY as attached — no recreating, redrawing, recoloring, simplifying, cropping, or regenerating it — in every application location outside the Launch screen, including:

The Login / Authentication page (replacing the transparent-X sliver currently shown there)
The logo + "XConnect" wordmark in the app header on every screen: Home, Roster, Analysis, Profile, Monitor, My Activity, etc., for all three roles
Any other in-app placement where the XConnect logo appears

Asset A must never appear anywhere outside the Launch screen going forward.

============================================================
2. PRESENTER HOME — ONE SCREEN, MATCHING ATTENDEE'S PATTERN

Current bug: Two different screens both currently act like a Presenter "home." The screen shown immediately after Presenter login is the CORRECT one, but it (a) is not bound to the bottom-nav "Home" item, and (b) is missing a Recent Sessions section. A second, separate screen (reached only via the "Home" nav button) shows unrelated technical SENSORS/SERVER details and a "Choose Room & Start" button — this second screen must be retired as a home destination.

Required end state — model this directly on the Attendee Home screen's pattern:

Single Home screen. The screen the Presenter already lands on immediately after login becomes the ONE AND ONLY Presenter Home, and the bottom-nav "Home" item must navigate to this exact same screen. No second, different-looking home screen may exist anywhere in the nav.
Animated status visual (top). Keep/use the circular X with continuously emitting concentric wave rings, exactly like the reference Attendee Home animation, with a "SPACE READY · [latency]" (or equivalent presenter-appropriate) status line beneath it — same visual language and motion as Attendee, not a static icon.
Primary action card (middle). The existing "Host / Anchor Room" card — icon, title, subtitle, room-selection chips (e.g. Hall A, Workshop 1, Auditorium), and the "Start Broadcasting" primary button — stays exactly as currently designed. No changes to this card.
Recent Sessions (new, below the primary card). Add a "RECENT SESSIONS" section directly beneath the Host/Anchor Room card, matching the Attendee Recent Sessions component exactly in structure and style:
Horizontally-scrollable row of cards
Each card: small icon badge (reuse the existing badge style), session/room name, date, duration, and a checkmark/status indicator for completed sessions
Populate with the Presenter's own hosted-session history (room, date, duration), not attendee data
Tapping a card opens the existing session-detail/history view already used for this data
Retire the second screen. The separate SENSORS/SERVER technical-detail screen with "Choose Room & Start" must no longer be reachable as a "Home" destination. If that sensor/server diagnostic content is still operationally needed, it may live later in the hosting flow (e.g., once a room is actively being anchored) — never as a duplicate landing/home screen.
============================================================
3. PROJECT CLEANUP — DELETE, DON'T JUST HIDE
Permanently delete the Admin PIN screen from the project files.
Permanently delete any other previously-rejected/abandoned screens still sitting in the project (including any earlier duplicate Presenter "home" variant no longer in use), even if currently unreachable through normal navigation.
Do not delete anything in active, approved use: Launch, Auth (with its existing Attendee/Presenter/Admin Login tabs and Google/Microsoft/Email sign-in — no PIN step), Attendee's three screens, Presenter's Home/Roster/Analysis/Profile and its sub-flows (Choose Room, Session Setup, Active Hosting, Full Roster, session-history detail), Admin's Monitor/Profile.
============================================================
4. FINAL ACCEPTANCE CHECKLIST

✓ Launch screen — completely unchanged, in every detail
✓ Attendee experience — completely unchanged
✓ Admin Monitor — completely unchanged
✓ Global theme/colors/navbar — unchanged in this pass
✓ Login page uses Asset B (square logo), never the transparent launch X
✓ Every in-app header logo uses Asset B, never the transparent launch X
✓ Launch animation still uses Asset A only, exclusively
✓ Presenter bottom-nav "Home" points to exactly one screen — the post-login landing screen
✓ That screen has: wave-emitting circular X animation + status line → Host/Anchor Room card (unchanged) → Recent Sessions section (new)
✓ Recent Sessions matches Attendee's Recent Sessions structure/style, populated with the Presenter's hosted-session data
✓ No second "Home"-bound screen with SENSORS/SERVER + "Choose Room & Start" remains
✓ Admin PIN screen physically deleted from the project
✓ Any other previously-rejected screens physically deleted from the project
✓ No other screen, for any role, altered beyond what is explicitly listed above

============================================================
FINAL INSTRUCTION

THIS IS A TARGETED FIX TO A FINISHED, APPROVED PRODUCT.

DO NOT REDESIGN.
DO NOT REBUILD.
DO NOT TOUCH THE LAUNCH SCREEN.
DO NOT TOUCH ATTENDEE OR ADMIN.

Make only the changes in Sections 1, 2, and 3 above. Everything else must come out of this exactly as it went in.